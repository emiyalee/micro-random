pipeline {
    agent {
        docker {
            image 'golang:1.13'
            args '-e GOPROXY=https://goproxy.cn'
        }
    }
    stages {
        stage('Build') {
            steps {
                sh 'echo $WORKSPACE'
                sh 'ls -al '
                sh 'go env'
            }
        }
    }
}